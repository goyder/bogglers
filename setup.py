from setuptools import setup, find_packages

setup(
    name='bogglers',
    version='0.1',
    author='Goyder',
    description='A deeply unnecessary rendition of a brute-force Boggle-search algorithm',
    packages=find_packages()
)